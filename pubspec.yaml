name: mobile
description: A new Flutter project.


publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  after_layout: ^1.1.0
  animations: ^2.0.0
  camera_camera: ^2.0.2
  file_picker_cross: ^4.4.2
  flutter_map: ^0.13.1
  hive: ^2.0.4
  hive_flutter: ^1.0.0
  image_picker: ^0.8.0+3
  latlong2: ^0.8.0
  archive: ^3.1.2
  snapping_sheet: ^3.1.0


  map_api:
    #    path: ../memorypath_map_api
    git:
      url: https://github.com/Memory-Path/memorypath_map_api.git
      ref: main
  memorypath_db_api:
    #    path: ../memorypath_database_api
    git:
      url: https://github.com/Memory-Path/memorypath_database_api.git
      ref: main

dev_dependencies:
  flutter_test:
    sdk: flutter

  dartdoc: ^0.45.0
  build_runner: ^2.0.4
  json_serializable: ^4.1.3

flutter:
  uses-material-design: true
  assets:
    - assets/images/
